ulw 이력서 V2 리뷰 기반 전체 개선

### 1. 기술 스택 31→10개 + 숙련도 구분
ko.json, en.json의 skills 섹션 수정:
- 주력 (5~6개): TypeScript, NestJS, AWS Lambda, MySQL, BigQuery, Docker
- 활용 (3~4개): Spring Boot, Redis, AWS CDK, Jest
- 경험: 나머지는 제거 (프로젝트 techStack에서만 노출)
- skills 카테고리를 "주력 기술", "활용 기술" 2개로 재구성
- en.json도 동일하게 동기화

### 2. 프로젝트 축소 (메인 이력서 전체)
experience 내 각 회사의 projects 배열에서 핵심만 남기고 나머지는 별도 필드로 분리.
방법: 각 프로젝트에 `featured: true/false` 필드 추가하거나, 또는 메인 템플릿에서 상위 N개만 표시.

추천 방식: 각 프로젝트에 `"featured": true` 추가. 템플릿에서 featured만 필터링.
경력상세 페이지(ExperienceDetailTemplate)에서는 전체 표시.

축소 대상:
- 게임듀오 6→3: 마케팅 통합 플랫폼, Lambda 마이그레이션, 공통 라이브러리 생태계
- 제이앤피메디 6→2: Maven Docs, Maven Auth
- 메드고 5→2: NestJS 아키텍처 전환, 의사/약사 웹 서비스
- 심플한 5→1: LMS 플랫폼
→ 총 8개 (웹 메인 + Print + ATS)

### 3. 메드고/심플한 highlights 숫자 보강
- 심플한: "수백 명" → 구체적 숫자 (300명 등, 데이터 확인 필요하면 합리적 추정)
- 메드고: STAR 형식 강화, 정량적 결과 추가

### 4. 4개월 공백 설명 (2024-09~12)
- 항해 플러스 교육 수료
- continuousLearning에 이미 있으면 기간 확인/수정
- 없으면 추가: "항해 플러스 백엔드 과정" (2024.09~12)
- experience 타임라인에서 자연스럽게 연결되도록

### 5. 메드고/심플한 highlights STAR 보강
- 상황(S) 부분 구체화
- 결과(R)에 숫자 추가

### 제약사항
- ko.json ↔ en.json 완전 동기화 필수
- 빌드 에러 없이 완료 (npm run build)
- 경력상세 페이지(ExperienceDetailTemplate)에서는 모든 프로젝트 표시
- 메인 이력서(ResumeTemplate, PrintTemplate, AtsTemplate)에서는 featured 프로젝트만 표시
- 기존 정량 수치 변경 금지
- featured 필터링 로직은 resume-data.ts 또는 각 템플릿에서 처리
- 최종 커밋 후 npm run build 확인
